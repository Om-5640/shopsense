fastapi>=0.115
uvicorn[standard]>=0.32
sse-starlette>=2.1
pydantic>=2.0
python-dotenv>=1.0
requests>=2.32
slowapi>=0.1.9

# v7: Deep Reddit via PRAW
praw>=7.7

# v7: Postgres + pgvector support (optional — falls back to SQLite if not installed)
psycopg2-binary>=2.9

# v9: Precise mention counting (Aho-Corasick multi-pattern scan)
pyahocorasick>=2.0.0

# Export (PDF generation — pure Python, no browser dependency)
fpdf2>=2.7.9

# CI / testing
pytest>=7.0
httpx>=0.27          # required by FastAPI TestClient (starlette.testclient)
